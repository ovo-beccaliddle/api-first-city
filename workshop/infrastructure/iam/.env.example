# IAM Service Environment Configuration

# Service port (externally exposed)
PORT=3000
EXTERNAL_PORT=3001

# Application settings
NODE_ENV=development
JWT_SECRET=workshop-secret-key
JWT_EXPIRATION=3600
TOKEN_ISSUER=city-services-iam

# Logging
LOG_LEVEL=info 